% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ma.R
\name{run_ma}
\alias{run_ma}
\title{Run a pairwise meta-analysis}
\usage{
run_ma(
  data,
  outcome_type = c("binary", "continuous"),
  sm = NULL,
  method = NULL,
  method.tau = c("REML", "DL"),
  random = TRUE,
  common = FALSE,
  hakn = NULL,
  prediction = NULL,
  incr = 0.5,
  subgroup = NULL,
  experimental_label = NULL,
  control_label = NULL
)
}
\arguments{
\item{data}{Canonical long-format data, typically from
\code{\link{ingest_data}}: one row per study x arm with columns
\code{studlab}, \code{treat}, \code{n}, plus \code{event} (binary) or \code{mean}/\code{sd} (continuous).}

\item{outcome_type}{One of \code{"binary"} or \code{"continuous"}.}

\item{sm}{Effect measure. Binary: \code{"OR"} or \code{"RR"}. Continuous: \code{"SMD"},
\code{"MD"}, or \code{"RoM"}. Defaults: \code{"OR"} for binary, \code{"SMD"} for continuous.}

\item{method}{Pooling method (binary only). One of \code{"Inverse"}, \code{"MH"},
\code{"Peto"}. Defaults: \code{"Inverse"} for OR, \code{"MH"} for RR.}

\item{method.tau}{Heterogeneity estimator. One of \code{"REML"} (default) or
\code{"DL"}.}

\item{random, common}{Logical. Use random-effects (default \code{TRUE}) and/or
common-effects (default \code{FALSE}) pooling.}

\item{hakn}{Hartung-Knapp-Sidik-Jonkman adjustment. NULL (default) uses
\code{TRUE} if k >= 3, else \code{FALSE}.}

\item{prediction}{Compute 95 percent prediction interval. NULL (default) uses
\code{TRUE} if k >= 3, else \code{FALSE}.}

\item{incr}{Continuity correction for zero events (binary). Default 0.5.}

\item{subgroup}{Optional column name in \code{data} for subgroup analysis.}

\item{experimental_label, control_label}{Values of \code{treat} identifying arms.
If NULL, the function looks for \code{"experimental"}/\code{"control"} first;
otherwise picks two distinct values from \code{treat}.}
}
\value{
An object of class \code{meta} (from the \{meta\} package).
}
\description{
Run a pairwise meta-analysis
}
