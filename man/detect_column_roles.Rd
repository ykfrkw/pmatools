% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_ingest.R
\name{detect_column_roles}
\alias{detect_column_roles}
\title{Report which column filled each ingest_data() role}
\usage{
detect_column_roles(data)
}
\arguments{
\item{data}{A data.frame, or a character vector of column names.}
}
\value{
A data.frame with one row per role, in role order, and columns:
\describe{
\item{role}{the canonical name (\code{studlab}, \code{treat}, \code{n}, \code{event},
\code{mean}, \code{sd}, \code{outcome}, \code{rob}, \code{indirectness}, \code{subgroup}).}
\item{column}{the source column that fills it, or \code{NA} when none does.}
\item{matched_by}{\code{"canonical"} when the column already carried the
role's own name, \code{"alias"} when it was renamed, \code{NA} when unfilled.}
\item{found}{whether the role is filled.}
\item{required}{whether \code{ingest_data()} aborts without it.}
}
}
\description{
Answers "did my data load correctly?" without loading it: given the columns
of a long-format table, says which of \code{\link{ingest_data}}'s canonical
roles were filled and by which source column. It resolves roles exactly as
\code{ingest_data(format = "long")} does -- same alias list, same order, same
first-role-wins rule -- so a role reported here as filled is a role
\code{ingest_data()} will fill.

Long format only. Wide input has fixed canonical pair names
(\code{event_e}/\code{event_c} and friends) and no alias mechanism to report on.
}
\examples{
detect_column_roles(c("study", "arm", "n_randomized", "d_r"))

}
