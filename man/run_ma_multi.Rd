% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_outcome.R
\name{run_ma_multi}
\alias{run_ma_multi}
\title{Run one meta-analysis per outcome}
\usage{
run_ma_multi(data, outcomes = NULL, sm = NULL, outcome_type = NULL, ...)
}
\arguments{
\item{data}{Canonical long-format data from \code{\link{ingest_data}},
containing an \code{outcome} column.}

\item{outcomes}{Character vector of outcomes to analyse, or \code{NULL}
(default) for every unique value of \code{data$outcome}. Names not present in
the data abort.}

\item{sm}{Effect measure: a single value applied to every outcome, or a
named vector/list keyed by outcome name. Outcomes not named fall back to
\code{run_ma()}'s default.}

\item{outcome_type}{\code{"binary"} / \code{"continuous"}, with the same
named-vector convention as \code{sm}. \code{NULL} (default) detects the type
per outcome from the columns that are populated.}

\item{...}{Further arguments passed to \code{\link{run_ma}} for every
outcome (for example \code{method.tau}, \code{random}, \code{incr}).}
}
\value{
A named list of \code{meta} objects (\code{NULL} for outcomes that
failed), carrying the source data as an attribute so
\code{\link{grade_meta_multi}} can pass it on to the resulting set.
}
\description{
Splits canonical long-format data on its \code{outcome} column and calls
\code{\link{run_ma}} once per outcome. \code{run_ma()} itself keeps
refusing multi-outcome data; this function is the supported way to analyse
several outcomes in one session.

An outcome whose meta-analysis fails (for example a single study on a
continuous outcome) is recorded as \code{NULL} and warned about, so the
remaining outcomes still complete.
}
\examples{
\dontrun{
d  <- ingest_data("outcomes_long.csv", format = "long")
ml <- run_ma_multi(d, sm = list("Mortality" = "RR", "Depression" = "SMD"))
}

}
