% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_bundle.R
\name{export_bundle}
\alias{export_bundle}
\alias{export_bundle.default}
\alias{export_bundle.pmatools}
\alias{export_bundle.meta}
\title{Export a reproducible analysis bundle as a ZIP}
\usage{
export_bundle(x, ...)

\method{export_bundle}{default}(x, ...)

\method{export_bundle}{pmatools}(x, ...)

\method{export_bundle}{meta}(
  x,
  grade,
  output_dir = ".",
  bundle_name = "pmatools_results",
  include = c("data", "script", "results", "forest", "forest_rob", "funnel",
    "funnel_trimfill", "pubias_missing_forest", "grade_table"),
  per = 1000,
  prediction = FALSE,
  convert_smd_to_or = FALSE,
  baseline_risk = NULL,
  threshold_label = NULL,
  chinn_invert = FALSE,
  other_text = NULL,
  other_downgrade = 0L,
  data = NULL,
  grade_args = NULL,
  ma_args = NULL,
  forest_display = NULL,
  rob = NULL,
  forest_display_rob = NULL,
  rare = NULL,
  rare_forest_display = NULL,
  pubias_missing_df = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{meta} object from \code{\link{run_ma}} (the single-outcome
entry point), a \code{pmatools} object, or a \code{pmatools_set}.}

\item{...}{Passed to the method.}

\item{grade}{A \code{pmatools} object from \code{\link{grade_meta}}.}

\item{output_dir}{Directory where the ZIP is created.}

\item{bundle_name}{Bundle base name (no extension).}

\item{include}{Which artifacts to include. See Details.}

\item{per}{Denominator for SoF rate columns. Default 1000.}

\item{prediction}{Show 95 percent prediction interval in SoF Effect column.}

\item{convert_smd_to_or}{Logical. Passed to \code{\link{sof_table}} for
continuous-outcome dichotomisation.}

\item{baseline_risk}{Numeric in (0,1). Passed to \code{\link{sof_table}}
when \code{convert_smd_to_or = TRUE}.}

\item{threshold_label}{Optional free-text label describing the
dichotomisation threshold.}

\item{chinn_invert}{Logical (default \code{FALSE}). Passed to
\code{\link{sof_table}}: flips the SMD sign before applying Chinn's
formula so that a negative-is-better SMD yields OR > 1 in the
dichotomised rate columns. Only relevant when
\code{convert_smd_to_or = TRUE}.}

\item{other_text}{Optional free-text "Other considerations" note passed to
\code{\link{evidence_profile}} for the bundled \code{grade_table.docx}.}

\item{other_downgrade}{Integer \code{0}/\code{-1}/\code{-2} (default
\code{0L}). Extra downgrade applied on top of the automatic domain
calculations; passed to \code{\link{evidence_profile}}.}

\item{data}{Optional canonical long-format tibble from
\code{\link{ingest_data}}; if provided, written to \code{data_long.csv}. If
NULL, the function attempts to reconstruct from \code{ma$data}.}

\item{grade_args}{Optional named list of \code{grade_meta()} argument
specifications with \code{value}/\code{origin}/\code{col} slots, used to render
\code{analysis.R} faithfully. \code{origin} must be one of \code{"null"}, \code{"column"},
\code{"scalar"}, or \code{"vector"}; any other value aborts rather than rendering
the argument as \code{NULL}. See SPEC.md.}

\item{ma_args}{Optional named list of \code{run_ma()} argument specifications.}

\item{forest_display}{Optional named list of arguments forwarded to
\code{\link{plot_forest}} when rendering the bundled forest plot.
Recognised names: \code{title}, \code{label_e}, \code{label_c}, \code{xlim},
\code{favors_left}, \code{favors_right}, \code{show_n}, \code{show_events},
\code{addrow_above}, \code{addrow_below}.}

\item{rob}{Optional character vector of per-study Risk-of-Bias labels
(length \code{length(meta_obj$studlab)} or \code{meta_obj$k}). Required
when \code{"forest_rob"} is in \code{include} to render the stratified forest plot.}

\item{forest_display_rob}{Optional named list of \code{plot_forest} arguments
for the RoB-stratified forest plot bundled when \code{"forest_rob"} is in
\code{include}. Same recognized names as \code{forest_display}.}

\item{rare}{Optional \code{pma_rare_meta} object from
\code{\link{run_rare_ma}}. When supplied, rare-event diagnostics,
the method table, and the method-comparison forest plot are bundled.}

\item{rare_forest_display}{Optional named list of display arguments for
\code{\link{plot_rare_sensitivity_forest}}.}

\item{pubias_missing_df}{Optional data.frame of studies with unavailable
results, with columns \code{studlab}, \code{n}, and \code{results_known}; forwarded to
\code{\link{plot_forest_pubias_subgroup}} when
\code{"pubias_missing_forest"} is in \code{include} (rendered only when k >= 10).}
}
\value{
Character. Absolute path to the created ZIP file.
}
\description{
Bundles every artifact of the analysis into a single ZIP: the long-format
CSV, a reproducible \code{analysis.R} script, results.txt, forest/funnel plots,
the SoF table, and the certainty appendix (Core GRADE series). The
bundled \code{analysis.R} runs
standalone with \code{library(pmatools)} and the bundled CSV.

Passing a \code{pmatools_set} from \code{\link{grade_meta_multi}} instead
produces the multi-outcome layout: the summary tables at the top level and
one numbered \verb{outcomes/NN_name/} sub-directory per outcome. See
\code{\link{export_bundle.pmatools_set}}.
}
\section{Legacy \verb{ma =} calls}{

Before version 0.5.0 \code{export_bundle()} was a plain function whose first
argument was named \code{ma}. It is now an S3 generic whose first argument is
\code{x}. Named calls of the form \code{export_bundle(ma = m, grade = g, ...)} are
still honoured, with a deprecation warning issued once per session; they
are dispatched as if \code{ma} had been passed as \code{x}. Update such calls to
pass the meta object positionally (or as \verb{x =}).
}

