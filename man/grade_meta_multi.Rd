% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_outcome.R
\name{grade_meta_multi}
\alias{grade_meta_multi}
\title{Rate certainty for several outcomes at once}
\usage{
grade_meta_multi(
  ma_list,
  common = list(),
  per_outcome = list(),
  data = NULL,
  primary = NULL
)
}
\arguments{
\item{ma_list}{Named list of \code{meta} objects, typically from
\code{\link{run_ma_multi}}. \code{NULL} elements (failed analyses) are
dropped.}

\item{common}{Named list of \code{\link{grade_meta}} arguments applied to
every outcome.}

\item{per_outcome}{Named list keyed by outcome name; each element is a named
list of \code{\link{grade_meta}} arguments that override \code{common} for that
outcome.}

\item{data}{Optional canonical long-format data stored on the set (used by
\code{\link{export_bundle}}). Defaults to the data recorded by
\code{run_ma_multi()}.}

\item{primary}{Character vector of primary outcome names. Default none.}
}
\value{
A \code{pmatools_set}.
}
\description{
Calls \code{\link{grade_meta}} once per element of \code{ma_list} and collects
the results into a \code{pmatools_set}. Arguments in \code{common} apply to every
outcome; \code{per_outcome} supplies (and overrides) arguments for one outcome.

An outcome that fails to rate is recorded as \code{NULL} with a warning, so
the rest of the batch completes. The Core GRADE 2 entry gate is the
exception: an abort caused by \code{threshold_type = "mid"} without a
threshold is re-raised unchanged, because a batch run must not become a way
around it.
}
\details{
\code{common} and \code{per_outcome} also accept two presentation arguments that
\code{grade_meta()} itself does not take: \code{follow_up} and \code{unit}. They are
stored on the rated object and picked up by
\code{grade_table(style = "bmj")}.
}
\examples{
\dontrun{
set <- grade_meta_multi(
  ml,
  common = list(study_design = "RCT", threshold_type = "null"),
  per_outcome = list("Mortality" = list(threshold = 1.25,
                                        threshold_type = "mid",
                                        threshold_scale = "ratio"))
)
}

}
